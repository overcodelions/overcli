# Pin this repo to the public npm registry so dependencies resolve to
# registry.npmjs.org regardless of a developer's global ~/.npmrc (e.g. a
# corporate Sonatype Nexus mirror). This keeps package-lock.json URLs
# reproducible and lets CI install without private-registry credentials.
registry=https://registry.npmjs.org/
